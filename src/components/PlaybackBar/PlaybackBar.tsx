import React from "react";
import "./PlaybackBar.css";

const PlaybackBar: React.FC = () => {
  return (
    <div className="playback-bar">
      {/* Aquí irán los controles de reproducción */}
    </div>
  );
};

export default PlaybackBar;
